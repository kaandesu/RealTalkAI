import type { Page } from '~/types/config'

export const Results = ref<Page>({
	title: 'Results',
	icon: 'oui:app-reporting',
	href: '/dashboard/dashboard',
	uid: '',
	desc: 'Review your performance with detailed feedback on flow, grammar, and vocabulary.',
	tourDesc:
		'Review your performance with detailed feedback on flow, grammar, and vocabulary.',
})
